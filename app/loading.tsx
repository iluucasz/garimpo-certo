export default function Loading(){return <main className="container section" aria-busy="true" aria-label="Carregando conteúdo"><div className="kicker">CARREGANDO</div><h1 style={{fontSize:48}}>Preparando boas escolhas…</h1><div className="product-grid">{Array.from({length:4}).map((_,i)=><div key={i} className="product-card"><div className="product-image"/><div className="product-info"><div className="progress"><span style={{width:`${55+i*8}%`}}/></div><div className="progress"><span style={{width:'80%'}}/></div></div></div>)}</div></main>}
